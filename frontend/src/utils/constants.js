const BASE_URL = "http://localhost:3001/api";

const columnTemplateHead = [
  "200px",
  "minmax(100px,150fr)",
  "minmax(100px,150fr)",
  "300px",
  "140px",
  "140px",
];

const columnTemplateBody = columnTemplateHead;

export { BASE_URL, columnTemplateBody, columnTemplateHead };
